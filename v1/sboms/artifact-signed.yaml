name: artifact-signed
description: "A rule to verify that the artifact is signed using an SBOM"
labels:
  - sboms
  - sample-policy-bundle
initiatives:
  - client-policy

evidence:
  format-type: cyclonedx-json
  signed: true

with:
  identity:
    common-names: []
    emails: []
