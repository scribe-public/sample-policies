name: vulnerabilities-report
path: scribe-api-cve.rego

description: "A rule to verify unmanaged vulnerabilities using ScribeHub API"

labels:
  - scribe
  - ct-6
  - ct-13
initiatives:
  - bundle-rule

evidence: 
  signed: false
  format-type: cyclonedx-json
  # predicate_type: https://cyclonedx.org/bom

with:
  superset:
    cve:
      max: 0
      severity: 6
      # component_name: "liblzma5"
      # component_version: "5.6.0"
