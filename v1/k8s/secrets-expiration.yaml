name: secrets-expiration
path: secrets-expiration.rego

level: warning

description: "A rule to verify that all secrets are not expired"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=project"
