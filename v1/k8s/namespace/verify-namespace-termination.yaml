name: Verify Namespace Termination
path: verify-namespace-termination.rego

level: warning

description: "The Verify Namespace Termination rule checks if specific namespaces have been terminated by using a list of regular expressions (regex) to match the namespaces and verify their termination status."

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=namespace"

with:
  // List of regex to check for namespace names
  namespaces: []
