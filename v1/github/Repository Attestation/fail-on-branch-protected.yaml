name: fail-on-branch-protected
path: fail-on-branch-protected.rego

level: warning

description: "A rule to verify that branch protected is set to the value defined in the configuration file."

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=repository"

with:
  predefined_protected: true # Will fail for branches that do not share the same value as this one.
