name: fail-on-secrets-older-than-k-months
path: fail-on-secrets-older-than-k-months.rego

level: warning

description: "A rule to verify that secrets are not older than k months"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=repository"

with:
  k: 12 # Enter an numerical value
