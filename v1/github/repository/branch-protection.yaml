name: branch-protection
path: branch-protection.rego

level: warning

description: "A rule to verify that branch protected is set to the desired value in the configuration file."

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=repository"

with:
  desired_protected: true
  
