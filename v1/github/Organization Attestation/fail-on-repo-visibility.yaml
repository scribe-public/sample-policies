name: fail-on-repo-visibility
path: fail-on-repo-visibility.rego

level: warning

description: "A rule to verify that only predefined public repos are permitted"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=organization"

with:
  allowed_repo_names: # Regex patterns
    - 