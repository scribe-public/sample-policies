name: required-image-labels
path: verify-labels-exist.rego

description: "Require specific image labels"
labels:
  - images
initiatives:
  - Default

aggregate-results: true

evidence:
  signed: false
  format-type: cyclonedx-json
  target_type: container

with:
  labels: []
