name: labels
path: verify-labels-exists.rego

description: "A rule to verify that the image has all the required labels"
labels:
  - images
  - sample-policy-bundle
initiatives:
  - client-policy

evidence:
  signed: false
  format-type: cyclonedx-json
  target_type: container

with:
  labels: []
