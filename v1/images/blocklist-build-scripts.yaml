name: disallowed-build-script
path: blocklist-build-scripts.rego

description: "Restrict commands in build scripts"
labels:
  - images
initiatives:
  - bundle-rule

evidence:
  signed: false
  format-type: cyclonedx-json
  target_type: container

with:
  blocklist: []
