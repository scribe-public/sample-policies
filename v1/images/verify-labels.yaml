name: labels
path: verify-labels.rego

description: "A rule to verify that the image has all the required labels keys and values"
labels:
  - images
initiatives:
  - bundle-rule

evidence:
  signed: false
  format-type: cyclonedx-json
  target_type: container

with:
  labels: []
