name: allowed-base-image
path: allowed-base-image.rego

description: "A rule to verify that Base image are allowed source"

labels:
  - images
initiatives:
  - Default

evidence:
  signed: false
  content_body_type: cyclonedx-json
  target_type: container


with:
  approved_sources: []
