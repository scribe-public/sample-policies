name: Command Line Input Check
path: cmd-line-input-check.rego

description: "Verify that no specific command lines have been used."

# ------------- Not sure if this is need ---------------------
# labels:
#   - scribe
#   - ct-6
#   - ct-13
# initiatives:
#   - bundle-rule

require-scribe-api: true

# ------------- Not sure if this is need ---------------------
# evidence:
#   signed: false
#   format-type: cyclonedx-json
#   # predicate_type: https://cyclonedx.org/bom

with:
  superset:
    filters:
      # Required Filters
      command_line_list: []             # list of strings for command lines that will be checked

      # Optional Filters
      mainAttestation_list: []          # list of strings for attestation that will be checked
      pipelineRun_list: []              # list of strings for pipelineRun that will be checked
      accepted_path_regex_list: []      # list of strings for accepted path regexes that will not be checked

