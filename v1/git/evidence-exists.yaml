name: git-evidence-exists
description: "A rule for checking that required evidence exists"
labels:
  - git
  - sample-policy-bundle
initiatives:
  - bundle-rule

evidence:
  signed: false
  format-type: cyclonedx-json
  target_type: git
