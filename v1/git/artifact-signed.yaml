name: git-artifact-signed
description: "Verify artifact is signed"
labels:
  - git
initiatives:
  - Default

evidence:
  signed: true
  format-type: cyclonedx-json
  target_type: git

with:
  identity:
    common-names: []
    emails: []
