name: git-artifact-signed
description: "Verify artifact is signed"
labels:
  - git
initiatives:
  - Default

evidence:
  filter-by:
    - product
    - target
  signed: true
  content_body_type: cyclonedx-json
  target_type: git

with:
  identity:
    common-names: []
    emails: []
