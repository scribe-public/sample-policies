name: slsa-evidence-exists
description: "A rule for checking that required evidence exists"
labels:
  - slsa
  - sample-policy-bundle
initiatives:
  - bundle-rule

evidence:
  signed: false
  format-type: slsa
  target_type: container
