name: slsa-artifact-signed
description: "A rule to verify that the artifact is signed"
labels:
  - slsa
initiatives:
  - bundle-rule

evidence:
  signed: true
  format-type: slsa
  target_type: container

with:
  identity:
    common-names: []
    emails: []
