name: slsa-provenance-signed
description: "Verify artifact is signed"
labels:
  - SLSA
initiatives:
  - Default

evidence:
  signed: true
  format-type: slsa
  target_type: container

with:
  identity:
    common-names: []
    emails: []
