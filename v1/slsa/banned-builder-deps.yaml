name: builder-unallowed-dependencies
path: banned-builder-deps.rego

description: "Verify artifact disallowed builder dependencies"
labels:
  - slsa
initiatives:
  - bundle-rule

evidence:
  signed: false
  format-type: slsa
  target_type: container

with:
  blocklist: []

