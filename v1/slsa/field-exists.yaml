name: field-exists
path: field-exists.rego

description: "Verify specified field exists in the artifact"
labels:
  - SLSA
initiatives:
  - Default

evidence:
  signed: false
  content_body_type: slsa
  target_type: container

with:
  paths: []
  violations_threshold: 0
