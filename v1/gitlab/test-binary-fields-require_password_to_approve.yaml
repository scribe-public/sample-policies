name: test-binary-fields-require_password_to_approve
path: test-binary-fields-require_password_to_approve.rego

level: warning

description: "Verify that the binary field: 'require_password_to_approve' is set correctly"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1 
  labels:
    - "asset_type=project" 

with:
  # You can configure the value of the binary field here
  require_password_to_approve: true


  

