name: secrets-scanning-pass-checker
path: secrets-scanning-pass-checker.rego

level: warning

description: "Verify that secrets scanning was succesful"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1 
  labels:
    - "asset_type=project" 

with:
  job_name: "secret_detection"
