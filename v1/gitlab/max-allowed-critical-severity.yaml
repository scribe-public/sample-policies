name: max-allowed-critical-severity
path: max-allowed-critical-severity.rego

level: warning

description: "Verify that the max allowed of critical severity is not passed"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1 
  labels:
    - "asset_type=project" 

with:
  max_allowed_vulnerability_count: # Enter here the max number of a the severity level
  severity: "Critical" # Enter here the severity of the vulnerabilty