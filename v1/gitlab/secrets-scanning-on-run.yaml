name: secrets-scanning-on-run
path: secrets-scanning-on-run.rego

level: warning

description: "Verify that secrets are not shared"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1 
  labels:
    - "asset_type=project" 

with:
  secret_detection_job_name: "secret_detection"
