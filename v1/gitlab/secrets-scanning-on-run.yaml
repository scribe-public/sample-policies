name: secrets-scanning
path: secrets-scanning.rego

level: warning

description: "Verify that secrets are not shared"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1 # not sure
  labels:
    - "asset_type=organization" # not sure

with:
    allowed_secrets_list: []
