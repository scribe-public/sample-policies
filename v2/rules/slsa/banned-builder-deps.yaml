config-type: rule
id: slsa-builder-unallowed-dependencies
name: "SLSA builder dependencies"
path: banned-builder-deps.rego

description: "Verify artifact disallowed builder dependencies"

labels:
  - SLSA
  - Image

evidence:
  signed: false
  content_body_type: slsa
  target_type: container

with:
  blocklist: []

