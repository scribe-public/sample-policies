config-type: rule
id: jenkins-instance-exists
name: Require Jenkins Instance Discovery Evidence
path: evidence-exists.rego

description: Verify the Jenkins Instance exists as evidence.

labels:
  - Jenkins
  - Instance

fail-on-missing-evidence: true

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=jenkins"
    - "asset_type=instance"
    - >-
      {{- if eq (index .Context "asset-type") "instance" -}}
      {{- asset_on_target (index .Context "asset-name") -}}
      {{- else -}}
      {{- asset_on_target nil -}}
      {{- end -}}
