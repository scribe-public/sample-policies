config-type: rule
id: jenkins-exposed-credentials
name: "Exposed Credentials"
path: exposed-credentials.rego

description: "Verify exposed credentials"

labels:
  - Jenkins
  - Folder

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=folder"
