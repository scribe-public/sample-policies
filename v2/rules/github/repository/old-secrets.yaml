config-type: rule
id: github-repo-old-secrets
name: "No old secrets in GitHub repository"
path: old-secrets.rego

description: "A rule to verify that secrets are not older than max_secret_age months"

labels:
  - GitHub
  - Repository

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=github"
    - "asset_type=repository"

with:
  max_secret_age: 12 # number of months
  
  