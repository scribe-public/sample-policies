config-type: rule
id: github-org-dependency-graph
name: "Dependency graph enabled for new repositories"
path: dependency-graph.rego

description: "A rule to verify that the binary field: dependency_graph_enabled_for_new_repositories is properly set"

labels:
  - GitHub
  - Organization

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=github"
    - "asset_type=organization"

with:
  desired_value: false
  
