config-type: rule
id: github-org-validity-checks
name: Verify Secret_Scanning_Validity_Checks_Enabled Setting
path: validity-checks.rego

description: Verify `secret_scanning_validity_checks` is configured in the GitHub repository.

labels:
  - GitHub
  - Organization

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=github"
    - "asset_type=organization"
    - >-
      {{- if eq (index .Context "asset-type") "organization" -}}
      {{- asset_on_target (index .Context "asset-name") -}}
      {{- else -}}
      {{- asset_on_target nil -}}
      {{- end -}}

with:
  desired_value: true
