config-type: rule
id: github-org-validity-checks
name: "GitHub organization secret scanning validity checks is properly set"
path: validity-checks.rego

description: "A rule to verify that the binary field: secret_scanning_validity_checks_enabled is properly set"

labels:
  - GitHub
  - Organization

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=github"
    - "asset_type=organization"

with:
  desired_value: false
  
