config-type: rule
id: gitlab-project-disallow-force-push
name: "GitLab project disallow force push"
path: force-push-protection.rego

description: "Verify that force push is not allowed for the project"

labels:
  - Gitlab
  - Project

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=project"

with:
  branch: "main"
