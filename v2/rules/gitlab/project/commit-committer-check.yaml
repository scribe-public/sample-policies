config-type: rule
id: gitlab-project-verify-commit_committer_check
name: "Verify commit_committer_check is enabled for GitLab project"
path: commit-committer-check.rego

description: "Verify that commit_committer_check is enabled for the project"

labels:
  - Gitlab
  - Project

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=project"
