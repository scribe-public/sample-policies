config-type: rule
id: gitlab-project-code-owner-approval
name: "Code owner approval required for GitLab project"
path: co-approval-required.rego

description: Verify that code owner approval is required for specific branches in the project"

labels:
  - Gitlab
  - Project

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=project"

with:
  branch: "main"
