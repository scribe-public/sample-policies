config-type: rule
id: gitlab-project-signed-commits
name: "Signed commits check in GitLab project"
path: check-signed-commits.rego

description: "Verify that all commits in the project are signed"

labels:
  - Gitlab
  - Project

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=project"
