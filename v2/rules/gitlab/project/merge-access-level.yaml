config-type: rule
id: gitlab-project-merge-access-level
name: "Merge access level policy check for GitLab project"
path: merge-access-level.rego

description: "Verify that the project's merge access level policy meets the requirements"

labels:
  - Gitlab
  - Project

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=project"

with:
  branch: "main"
  # access_level: 30
  access_level_description: "Maintainers"
