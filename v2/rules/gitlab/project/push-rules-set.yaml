config-type: rule
id: gitlab-project-verify-push_rules
name: "Push rules set for GitLab project"
path: push-rules-set.rego

description: "Verify that push rules are set for the project"

labels:
  - Gitlab
  - Project

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=project"
