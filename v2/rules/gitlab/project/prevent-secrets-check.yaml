config-type: rule
id: gitlab-project-verify-prevent_secrets
name: "GitLab project prevent_secrets check"
path: prevent-secrets-check.rego

description: "Verify that prevent_secrets is enabled for the project"

labels:
  - Gitlab
  - Project

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=project"
