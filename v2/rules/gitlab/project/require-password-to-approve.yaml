config-type: rule
id: gitlab-project-require-password-to-approve
name: "Require password to approve in GitLab project"
path: require-password-to-approve.rego

description: "Verify that the binary field: 'require_password_to_approve' is set correctly"

labels:
  - Gitlab
  - Project

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=project"

with:
  require_password_to_approve: true
