config-type: rule
id: gitlab-org-allowed-visible-projects
name: "GitLab organization allowed visible projects"
path: projects-visibility.rego

description: "Verify that only allowed projects have public visibility"

labels:
  - Gitlab
  - Organization

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=gitlab"
    - "asset_type=organization"

with:
    allowed_public:
        - "Learn GitLab"
        - "Scribe Gitlab Test"