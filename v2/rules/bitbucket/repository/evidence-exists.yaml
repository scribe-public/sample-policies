config-type: rule
id: bb-repo-exists
name: Require Bitbucket Repository Discovery Evidence
path: evidence-exists.rego

description: Verify the Bitbucket Repository exists as evidence.

labels:
  - Bitbucket
  - Repository

fail-on-missing-evidence: true

evidence:
  signed: false
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "platform=bitbucket"
    - "asset_type=repository"
    - >-
      {{- if eq (index .Context "asset-type") "repository" -}}
      {{- asset_on_target (index .Context "asset-name") -}}
      {{- else -}}
      {{- asset_on_target nil -}}
      {{- end -}}
