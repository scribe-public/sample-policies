config-type: rule
id: generic-artifact-signed
name: Generic Artifact Signed
path: artifact-signed.rego

description: Verify required evidence is signed.

labels:
  - 3rd-party

fail-on-missing-evidence: true

evidence:
  signed: true
  content_body_type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/generic/v0.1
