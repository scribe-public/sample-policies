name: "SSDF-REPO"
defaults:
  evidence:
    signed: false
    labels:
      - '{{ .Args.RepositoryAsset }}'

rules:
  - uses: ssdf/ps-3.1-release-archived@frameworks

  - uses: ssdf/ps-1-branch-protection@frameworks
    level: warning

  - uses: ssdf/ps-1-repo-private@frameworks
    level: warning
    