name: "Project Policy"
defaults:
  evidence:
    signed: false
    labels:
      - "asset_type=project"
      - '{{ .Args.ProjectAsset }}'
rules:
  - uses: project/abandoned-project@discovery
  - uses: project/push-rules-set@discovery
  - uses: project/approvals-policy-check@discovery
  - uses: project/author-email-regex@discovery
  - uses: project/commit-committer-check@discovery
  - uses: project/member-check@discovery
  - uses: project/prevent-secrets-check@discovery
  - uses: project/reject-unsigned-commits@discovery
  - uses: project/visibility-check@discovery
  - uses: project/push-access-level@discovery
  - uses: project/merge-access-level@discovery
  - uses: project/force-push-protection@discovery
  - uses: project/co-approval-required@discovery
  - uses: project/commit-author-email-check@discovery
  - uses: project/commit-author-name-check@discovery
  - uses: project/commit-message-check@discovery
  - uses: project/commits-validated@discovery
  - uses: project/committer-email-check@discovery
  - uses: project/committer-name-check@discovery