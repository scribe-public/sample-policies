name: "SSDF-IMAGE"
defaults:
  evidence:
    signed: false

rules:
  - uses: ssdf/ps-2-image-verifiable@frameworks
    level: warning

  - uses: ssdf/ps-3.2-archived-sbom@frameworks
    level: warning
