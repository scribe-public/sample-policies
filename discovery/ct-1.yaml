name: "CT-1"
defaults:
  evidence:
    signed: false
    labels:
      - "asset_type=organization"
      - '{{ .Args.OrganizationAsset }}'
rules:
  - name: "CT-1.1"
    uses: org/max-admins@discovery
    with:
      max_admins: 1
