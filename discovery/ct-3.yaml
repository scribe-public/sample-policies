name: "CT-3"
defaults:
  evidence:
    signed: false
    labels:
      - "asset_type=organization"
      - '{{ .Args.OrganizationAsset }}'
rules:
  - name: "CT-3.1"
    uses: org/longlive-tokens@discovery
