name: "CT-2"
defaults:
  evidence:
    signed: true
    labels:
      - "asset_type=project"
      - '{{ .Args.ProjectAsset }}'
rules:
  - uses: project/reject-unsigned-commits@discovery
    level: warning

  - uses: project/check-signed-commits@discovery
    level: warning
