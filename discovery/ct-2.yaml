name: "CT-2"
defaults:
  evidence:
    signed: true
    labels:
      - "asset_type=project"
      - '{{ .Args.ProjectAsset }}'
rules:
  - name: "CT-2.1"
    uses: project/reject-unsigned-commits@discovery