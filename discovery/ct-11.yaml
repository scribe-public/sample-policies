name: "CT-11"
defaults:
  evidence:
    signed: true
rules:
  - uses: api/scribe-api-cve@v1
    description: "[CT-11] Verify no critical or high CVEs"
    level: warning
    with:
      superset:
        cve:
          severity: 6
          max: 0
