name: "SSDF-ORG"
defaults:
  evidence:
    signed: false
    labels:
      - '{{ .Args.OrganizationAsset }}'

rules:
  - uses: ssdf/ps-1-2fa@frameworks
    level: warning

  - uses: ssdf/ps-1-max-admins@frameworks
    level: warning

  - uses: ssdf/ps-1-web-commit-signoff@frameworks
    level: warning
