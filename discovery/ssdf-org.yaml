name: "SSDF-ORG"
defaults:
  evidence:
    signed: false
    labels:
      - '{{ .Args.OrganizationAsset }}'

rules:
  - uses: ps-1-2fa@ssdf
    level: warning

  - uses: ps-1-max-admins@ssdf
    level: warning

  - uses: ps-1-web-commit-signoff@ssdf
    level: warning
