name: visibility-check
path: visibility-check.rego

level: warning

description: "A rule to verify that project visibility matches the required visibility level"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=project"

with:
  visibility: private