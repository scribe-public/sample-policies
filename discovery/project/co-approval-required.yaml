name: co-approval-required
path: co-approval-required.rego

level: warning

description: "A rule to verify that code owner approval is required for specific branch in the project"
labels:
  - venafi

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=project"

with:
    branch_name: "main"