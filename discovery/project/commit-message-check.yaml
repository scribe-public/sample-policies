name: commit-message-check
path: commit-message-check.rego

level: warning

description: "A rule to verify that all commit messages meet the regex"

evidence:
  signed: false
  format-type: generic
  target_type: data
  predicate_type: http://scribesecurity.com/evidence/discovery/v0.1
  labels:
    - "asset_type=project"

with:
    commit_message_regex: ".*JIRA-\\d+.*"